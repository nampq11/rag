[project]
name = "rag-benchmark"
version = "0.1.0"
description = "Local-only Ragas retrieval benchmark tooling"
requires-python = ">=3.11"
dependencies = [
    "httpx==0.28.1",
    "langchain-community==0.3.27",
    "langchain-ollama==0.2.3",
    "langchain-postgres==0.0.17",
    "langchain==0.3.30",
    "langchain-core==0.3.86",
    "PyJWT==2.13.0",
    "ragas==0.4.3",
]

[tool.uv]
package = false
